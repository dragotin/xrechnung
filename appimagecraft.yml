version: 1

project:
  name: de.freisturz.opensource.desktop
  version_command: git describe --tags

build:
  cmake:

appimage:
  linuxdeploy:
    plugins:
      - qt
    environment:
      UPD_INFO: "gh-releases-zsync|dragotin|xrechnung|latest|xrechnung-*x86_64.AppImage.zsync"

