version: 1

project:
  name: de.volle_kraft_voraus.xrview.desktop
  version_command: cd "$PROJECT_ROOT" && git rev-parse --short HEAD

build:
  cmake:

appimage:
  linuxdeploy:
    plugins:
      - qt
    environment:
      UPD_INFO: "gh-releases-zsync|dragotin|xrechnung|latest|xrechnung-*x86_64.AppImage.zsync"

